version: 1
backend:
  phases:
    preBuild:
      commands:
        - echo "Skipping backend preBuild in hosted pipeline"
    build:
      commands:
        - echo "Skipping backend build; backend managed outside Amplify Hosting"
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
